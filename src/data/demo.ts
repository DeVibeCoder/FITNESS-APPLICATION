/**
 * Credentials for the three seeded demo accounts.
 *
 * This is sample data for a local, offline demo — the same category of thing
 * as the seeded weights and workouts, not a secret. It is shown on the sign-in
 * screen because otherwise nobody could get into their own demo. Real accounts
 * created through setup choose their own password, and when a backend arrives
 * this file goes with the rest of the seed.
 */
export const DEMO_PASSWORD = 'circuit2026'

export const DEMO_HANDLES = ['ahmed', 'nadia', 'samir'] as const
